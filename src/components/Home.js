import React from "react";

const Home = (props) => {
    return(
        <div>
            <div>
                <img src={props.image} alt='' />
            </div>
        </div>
    )
}

export default Home;